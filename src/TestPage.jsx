// Файл: src/TestPage.jsx - АБСОЛЮТНО ПРОСТАЯ СТРАНИЦА ДЛЯ ТЕСТА
import { useEffect, useState } from 'react';

function TestPage() {
  const [timer, setTimer] = useState(0);

  useEffect(() => {
    // Простой таймер, который обновляется каждую секунду
    // Если страница "упадёт", таймер остановится.
    const interval = setInterval(() => {
      setTimer(prev => prev + 1);
    }, 1000);

    // Логируем факт запуска
    console.log('🟢 TestPage смонтирован. Таймер запущен.');

    // Очистка при размонтировании
    return () => {
      clearInterval(interval);
      console.log('🔴 TestPage размонтирован. Таймер остановлен.');
    };
  }, []);

  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h1>✅ Тест стабильности Vercel</h1>
      <p>Эта страница работает уже: <strong>{timer}</strong> секунд.</p>
      <p>Если число растёт — страница жива. Если остановилось — страница упала.</p>
      <p>Текущее время: {new Date().toLocaleTimeString()}</p>
    </div>
  );
}

export default TestPage;